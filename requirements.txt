# 核心图像处理
opencv-python-headless==4.9.0.80  # 使用 headless 版本，兼容 NumPy 1.x
scikit-image==0.25.2
imgaug==0.4.0
pyclipper==1.3.0.post6
albumentations~=1.4.10

# 数据处理和序列化
numpy==1.26.4  # 降级以兼容 PyTorch 和 OpenCV
pandas==2.2.3
PyYAML==6.0.2
lmdb==1.6.2
protobuf==6.30.2
webdataset==0.2.111

# 配置管理
omegaconf==2.3.0
easydict==1.13

# 机器学习框架
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0
scikit-learn==1.6.1

# 视频处理
av==14.3.0

# PaddlePaddle 相关
paddlepaddle-gpu==2.5.0.post118  # 明确指定 CUDA 11.8 版本
paddleocr==2.10.0
paddle2onnx==1.3.1

# ONNX 运行时
onnxruntime-gpu==1.15.1  # 支持 CUDA 11.8 的最后一个版本

# 其他工具
tqdm==4.67.1
einops==0.8.1
filesplit==3.0.2
gradio

# 特定平台依赖
onnxruntime-directml==1.15.1; sys_platform == 'win32'  # 保持版本一致
PySimpleGUI-4-foss==4.60.4.1; sys_platform == 'win32'
